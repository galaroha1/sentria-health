rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ALLOW EVERYTHING (Test Mode)
    // WARNING: This allows anyone with your project ID to read/write your database.
    // Only use this for development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
